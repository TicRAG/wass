ccr: 1.0
large_sizes:
- 200
- 500
medium_sizes:
- 50
- 100
patterns:
- montage
- highly_parallel
random_seed: 42
small_sizes:
- 5
- 10
- 15
- 20
workflow_dir: workflows

# Configuration for generating experiment workflows (for final testing)
experiment_workflows:
  montage:
    sizes: [20, 50, 100]
    count: 3
    seed_start: 1
  highly_parallel:
    sizes: [20, 50, 100]
    count: 3
    seed_start: 1

# Configuration for generating training workflows (for the knowledge base)
# We are significantly increasing the size and variety here.
training_workflows:
  montage:
    sizes: [50, 100, 200]    # Added a larger size
    count: 25               # Increased count from 3 to 25
    seed_start: 100         # Use a different seed to avoid overlap with tests
  highly_parallel:
    sizes: [50, 100, 200]    # Added a larger size
    count: 25               # Increased count from 3 to 25
    seed_start: 100         # Use a different seed