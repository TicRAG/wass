# configs/workflow_config.yaml

ccr: 1.0
large_sizes:
- 200
- 500
medium_sizes:
- 50
- 100
patterns:
- montage
- highly_parallel
random_seed: 42
small_sizes:
- 5
- 10
- 15
- 20
workflow_dir: workflows

synthetic_dir: data/workflows/synthetic

training_workflows:
  # comm_intensive:
  #   sizes: [20,40] 
  #   count: 2
  #   seed_start: 100
  pipeline:
    sizes: [100]
    count: 1
    seed_start: 100
  # montage:
  #   sizes: [20,40] 
  #   count: 6
  #   seed_start: 2 

experiment_workflows:
  # comm_intensive:
  #   sizes: [1000]
  #   count: 1
  #   seed_start: 100
  pipeline:
    sizes: [100]
    count: 1
    seed_start: 100
  synthetic:
    files:
      - synthetic_workflow_000.json
      - synthetic_workflow_001.json
      - synthetic_workflow_002.json
      - synthetic_workflow_003.json
      - synthetic_workflow_004.json
  # montage:
  #   sizes: [20,40]
  #   count: 6
  #   seed_start: 1

# -----------------------------------------------------------------------------
# Platform XML configuration
# Added to make previously hard-coded references (e.g. configs/platform_small.xml)
# configurable. Code can now read:
#   cfg['platform_xml']['base_dir'] + '/' + cfg['platform_xml']['mapping'][cfg['platform_xml']['default']]
# or pick a size-specific key dynamically.
#
# Fields:
#   base_dir : directory where the platform XML files reside (relative or absolute)
#   mapping  : logical name -> XML filename
#   default  : logical name to use when no explicit selection is provided
# -----------------------------------------------------------------------------
platform_xml:
  base_dir: configs
  mapping:
    small: platform_small.xml
    medium: platform_medium.xml
    medium_hetero: platform_medium_hetero.xml
    tight: platform_tight.xml
    extreme_hetero: platform_extreme_hetero.xml
    large: platform_large.xml
    test: test_platform.xml
    ultra_bias: platform_ultra_bias.xml
    ultra_bias_slow: platform_ultra_bias_slow.xml
  default: extreme_hetero

